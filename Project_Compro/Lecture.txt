📝 Outline เอกสารข้อเสนอโครงงาน

Car Rent System using Python File I/O (Struct-based Fixed-Length Records)

1) ชื่อโครงการ & ทีมงาน

ชื่อโครงการ: Car Rent System – Binary File I/O with Struct

ทีมงาน: (ใส่ชื่อสมาชิก ≤ 4 คน)

บทบาท: Programmer / Tester / Documentation

2) บริบทและขอบเขตปัญหา

ปัญหา: ข้อมูลรถเช่ามักเก็บใน Excel/CSV → เสี่ยงข้อมูลเสียหาย, ขาดประสิทธิภาพในการค้นหา, ไม่มีประวัติ CRUD

โครงงานนี้: ใช้ ไฟล์ไบนารีแบบ fixed-length record ด้วย struct เพื่อให้การเก็บและเข้าถึงข้อมูลมีความเสถียร, มีการตรวจสอบความถูกต้อง, และรองรับการสร้างรายงาน

3) ข้อกำหนดเชิงหน้าที่ (Functional Requirements)

เมนูหลัก:

Add (เพิ่มรถใหม่)

Update (แก้ไขข้อมูล เช่น ค่าเช่า, สถานะ)

Delete (ลบ → เปลี่ยนสถานะเป็น Deleted)

View (ดูรายการรถ)

ดูรายการเดียว

ดูทั้งหมด

ดูแบบกรอง (เช่น เฉพาะ Active หรือ เฉพาะแบรนด์)

สรุปสถิติ

Generate Report (.txt)

Exit (บันทึก/ปิดระบบ)

4) สเปกไฟล์และระเบียน
Master file (cars.dat)
car_id        : int (4)  
status        : int (1=Active,0=Deleted)  
is_rented     : int (1/0)  
year          : int (4)  
daily_rate_thb: float (4)  
odometer_km   : int (4)  
license_plate : 12s  
brand         : 12s  
model         : 16s  
created_at    : int (timestamp)  
updated_at    : int (timestamp)  

Event log (cars.log)
ts             : int (timestamp)  
op_code        : int (1=ADD,2=UPDATE,3=DELETE,4=VIEW)  
car_id         : int  
status_after   : int (1/0)  
is_rented_after: int (1/0)  
rate_after_thb : float  

Index/Free-list file (cars.idx)

เก็บตำแหน่ง record ที่ถูกลบเพื่อ reuse

Report (report.txt)

Header (เวอร์ชัน, เวลาสร้าง, encoding)

ตารางรถ

Summary (Active, Deleted, Available, Rented)

Rate statistics (min/max/avg)

Cars by Brand

5) ผังการทำงาน (Flow)

Add: รับ input → Validate → Pack struct → Append file → Log

Update: Search by car_id → Modify field → Overwrite → Log

Delete: Search → Mark status=0 → Free-list → Log

View: Read all/filtered → Print table/summary

6) แผนการรายงาน

ออกรายงานเป็น .txt เมื่อผู้ใช้เลือกเมนู 5 หรือปิดโปรแกรม

รูปแบบเหมือนตัวอย่างที่แนบมา (มีสรุป, ค่า min/max/avg, นับตามแบรนด์)

7) แผนการทดสอบ

Dataset ≥ 50 records

ทดสอบ CRUD ปกติ

ทดสอบ input ผิด (ปีเป็นตัวอักษร, ค่าเช่าติดลบ)

ทดสอบ car_id ไม่พบ

จำลองไฟล์เสีย (แก้ byte ตรงกลางไฟล์)

8) หมุดหมายและกำหนดการ

M1: สรุปรูปแบบ record & ตัวอย่างข้อมูล

M2: ทดลองอ่าน/เขียน struct

M3: ดัชนี & free-list

M4: รวม CRUD & Report

M5: ทดสอบ + Finalize

9) ความเสี่ยง & การบรรเทา

Alignment struct → ใช้ format string ชัดเจน (<iif12s12s16s)

Encoding ป้ายทะเบียน/ชื่อรถ → ตัด/เติมเป็น fixed-length UTF-8

Data loss → มี log ทุกครั้งที่ทำ CRUD

10) ภาคผนวก

ตารางโครงสร้าง record (Master + Log)

แผนภาพเมนู

📌 ความน่าสนใจ:

เป็นโปรเจกต์ที่ practical (ใกล้เคียงระบบจริง)

มีการใช้ struct + binary file I/O ซึ่งไม่ค่อยเจอบ่อยในงานระดับนักศึกษา → ทำให้แตกต่างจากงาน CRUD ธรรมดาที่ใช้ SQLite/CSV

สามารถขยายต่อยอด เช่น เพิ่มระบบลูกค้า/สัญญาเช่า
